---
dependency:
  name: galaxy
driver:
  name: hetznercloud
platforms:
  - name: instance
    image: centos:${TEST_CENTOS_VERSION}
provisioner:
  name: ansible
scenario:
  name: ansible-galaxy
verifier:
  name: testinfra
